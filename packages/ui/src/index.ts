// Shared UI primitives re-exported for both web and admin apps.
// Apps may also import shadcn/ui components directly — this package
// exists for cross-app components that differ only in accent color
// (accent is controlled via CSS variables, so the same component works
// in both the blue user portal and the purple admin panel).

export { Button } from './button'
export { Input } from './input'
export { Badge } from './badge'
export { Card } from './card'
export { Spinner } from './spinner'
